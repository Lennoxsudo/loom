/**
 * 应用级配置常量
 */

export const APP_CONFIG = {
  appName: 'Loom',
  version: '1.0.0',
  maxOpenFiles: 50,
  maxRecentFiles: 20,
  maxTerminalInstances: 5,
  fileWatcherDebounceMs: 100,
  autoRefreshIntervalMs: 5000,
};
