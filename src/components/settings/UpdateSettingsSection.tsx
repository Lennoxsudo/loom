/**
 * Legacy row-based update block. The dedicated Settings → Updates page uses
 * `UpdateContent` (status-stage layout). Kept only if another surface imports it.
 */
export { UpdateContent as default } from './UpdateContent';
